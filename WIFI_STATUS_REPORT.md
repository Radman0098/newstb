# گزارش بررسی وضعیت وای‌فای
**تاریخ بررسی**: 2025-12-18 12:18
**سیستم**: Pop!_OS Linux 6.17.9-76061709-generic
**مادربرد**: MSI X870 GAMING PLUS WIFI (MS-7E47)

---

## 📊 خلاصه وضعیت

| مورد | وضعیت | توضیحات |
|------|-------|---------|
| درایور ath12k | ✅ بارگذاری شده | ماژول در کرنل موجود است |
| Firmware | ✅ نصب شده | linux-firmware نصب است |
| رادیو وای‌فای | ✅ فعال | nmcli radio wifi: enabled |
| دستگاه PCI | ❌ **شناسایی نشده** | هیچ دستگاه وای‌فای در PCI یافت نشد |
| اینترفیس شبکه | ❌ **وجود ندارد** | wlp8s0 یا wlan0 وجود ندارد |
| NetworkManager | ✅ در حال اجرا | سرویس فعال است |

---

## 🔍 جزئیات بررسی

### 1. دستگاه‌های شبکه شناسایی شده
```
DEVICE           TYPE      STATE                   CONNECTION         
enx026f5b59324b  ethernet  connected               Wired connection 2 
lo               loopback  connected (externally)  lo                 
enp11s0          ethernet  unavailable             --                 
```

**نتیجه**: هیچ دستگاه وای‌فای (wifi/wlan) شناسایی نشده است.

### 2. ماژول‌های کرنل
```
ath12k                958464  0
qmi_helpers            28672  1 ath12k
mac80211             1847296  1 ath12k
cfg80211             1462272  2 ath12k,mac80211
libarc4                12288  1 mac80211
qcom_scm               53248  1 ath12k
mhi                   139264  1 ath12k
```

**نتیجه**: درایورها به درستی بارگذاری شده‌اند اما دستگاه سخت‌افزاری برای استفاده از آن‌ها وجود ندارد.

### 3. بررسی PCI Devices
```bash
lspci -nn | grep -i "network\|wireless\|wifi"
# نتیجه: خالی - هیچ دستگاه وای‌فای یافت نشد
```

**نتیجه**: دستگاه وای‌فای در سطح سخت‌افزاری (PCI) شناسایی نمی‌شود.

### 4. بررسی اینترفیس‌های شبکه
```bash
ip link show
# فقط lo, enp11s0, enx026f5b59324b وجود دارند
# هیچ wlp8s0 یا wlan0 وجود ندارد
```

### 5. بررسی rfkill
```
0: hci0: Bluetooth
        Soft blocked: no
        Hard blocked: no
```

**نتیجه**: هیچ block نرم یا سخت برای وای‌فای وجود ندارد (چون دستگاه اصلاً شناسایی نشده).

### 6. بررسی درایور
```
filename: /lib/modules/6.17.9-76061709-generic/kernel/drivers/net/wireless/ath/ath12k/ath12k.ko.zst
description: Driver support for Qualcomm Technologies 802.11be WLAN devices
```

**نتیجه**: درایور برای Qualcomm Wi-Fi 7 (802.11be) موجود است.

---

## ⚠️ مشکل اصلی

**دستگاه وای‌فای در سطح سخت‌افزاری (PCI) شناسایی نمی‌شود.**

بر اساس لاگ‌های قبلی سیستم، وای‌فای قبلاً کار می‌کرده و اینترفیس `wlp8s0` وجود داشته که به شبکه `ali-5G` متصل بوده است. این نشان می‌دهد:

1. ✅ سخت‌افزار وای‌فای روی مادربرد وجود دارد
2. ✅ درایورها و firmware نصب هستند
3. ❌ **وای‌فای در BIOS غیرفعال شده است** (احتمال قوی)

---

## 🔧 راه‌حل‌های پیشنهادی

### راه‌حل 1: فعال‌سازی وای‌فای در BIOS (اولویت اول - 95% احتمال)

1. سیستم را **ریستارت** کنید
2. هنگام بوت، کلید **Delete** یا **F2** را فشار دهید
3. وارد بخش **Advanced** یا **Integrated Peripherals** شوید
4. گزینه **WiFi** یا **Wireless LAN** یا **WLAN** را پیدا کنید
5. آن را **Enabled** کنید
6. تغییرات را ذخیره کنید (**F10**)
7. سیستم را ریستارت کنید

**مسیر احتمالی در BIOS MSI**:
- `Advanced` → `Integrated Peripherals` → `WiFi` → `Enabled`
- یا `Settings` → `Advanced` → `Onboard Devices` → `WiFi` → `Enabled`

### راه‌حل 2: بررسی اتصال فیزیکی آنتن‌ها

برخی مادربردها نیاز به اتصال آنتن‌های وای‌فای دارند:
- بررسی کنید که آنتن‌های وای‌فای به درستی به پورت‌های `ANT1` و `ANT2` روی مادربرد وصل شده‌اند

### راه‌حل 3: به‌روزرسانی BIOS

اگر وای‌فای در BIOS فعال است اما کار نمی‌کند:
- به سایت MSI بروید و آخرین نسخه BIOS را دانلود کنید
- BIOS را به‌روزرسانی کنید

### راه‌حل 4: استفاده از USB WiFi Adapter (موقت)

اگر نیاز فوری به وای‌فای دارید:
- یک USB WiFi Adapter خریداری کنید
- سیستم به صورت خودکار آن را شناسایی می‌کند

---

## 📝 دستورات مفید برای بررسی بعدی

پس از فعال‌سازی وای‌فای در BIOS، این دستورات را اجرا کنید:

```bash
# بررسی دستگاه‌های شبکه
nmcli device status

# بررسی PCI devices
lspci | grep -i network

# بررسی اینترفیس‌های شبکه
ip link show

# بررسی ماژول‌های وای‌فای
lsmod | grep ath12k

# اجرای اسکریپت رفع مشکل
sudo /home/asr/Desktop/stb/scripts/fix_wifi.sh
```

---

## 📞 اطلاعات برای پشتیبانی

اگر مشکل حل نشد، این اطلاعات را جمع‌آوری کنید:

```bash
# اطلاعات سیستم
uname -a > system_info.txt
lspci -nn > pci_devices.txt
lsusb > usb_devices.txt
sudo dmesg > kernel_messages.txt
journalctl -u NetworkManager > network_manager.log
```

---

## ✅ نتیجه‌گیری

**وضعیت فعلی**: وای‌فای به دلیل عدم شناسایی در سطح سخت‌افزاری کار نمی‌کند.

**اقدام مورد نیاز**: فعال‌سازی وای‌فای در BIOS (احتمال 95%)

**پس از فعال‌سازی**: سیستم باید به صورت خودکار دستگاه را شناسایی کند و اینترفیس `wlp8s0` ایجاد شود.

---

**تاریخ گزارش**: 2025-12-18 12:18  
**تهیه شده توسط**: سیستم بررسی خودکار وای‌فای



