# راهنمای بازسازی کامل مدل STB5

این بکاپ شامل تمام فایل‌های ضروری برای بازسازی دقیق مدل STB5 است.

## 📋 محتویات بکاپ

### ✅ شامل شده (برای بازسازی کامل):

1. **کدهای منبع** (`src/`)
   - تمام ماژول‌های Python
   - Feature extractors
   - Model engine
   - Execution modules

2. **مدل‌های آموزش دیده** (`models/`)
   - تمام فایل‌های `.joblib` (XGBoost, CatBoost)
   - تمام فایل‌های `.pt` (PyTorch models: LSTM, PatchTST)
   - Metadata و گزارش‌های مدل
   - BlindSpot filters
   - Memory banks

3. **داده‌ها**
   - `data/raw/`: داده‌های خام برای آموزش
   - `data/processed/`: داده‌های پردازش شده

4. **پیکربندی‌ها** (`config/`)
   - تنظیمات استراتژی
   - پارامترهای بهینه
   - تنظیمات مدل

5. **پایگاه داده Optuna** (`optuna_*.db`)
   - نتایج بهینه‌سازی hyperparameters

6. **اطلاعات CatBoost** (`catboost_info/`)
   - اطلاعات آموزش CatBoost

7. **مستندات**
   - تمام فایل‌های Markdown
   - گزارش‌های تحلیل
   - راهنماها

## 🔧 مراحل بازسازی

### 1. استخراج بکاپ
```bash
unzip STB5_FULL_BACKUP_*.zip
cd STB5_FULL_BACKUP_*
```

### 2. ایجاد محیط مجازی
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# یا
venv\Scripts\activate  # Windows
```

### 3. نصب وابستگی‌ها
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. بررسی نصب
```bash
python scripts/check_ml_stack.py
```

### 5. بررسی مدل‌ها
```bash
# بررسی وجود مدل‌ها
ls -lh models/*.joblib
ls -lh models/lstm/*.pt
ls -lh models/patchtst/*.pt
```

### 6. بارگذاری و استفاده از مدل‌ها
```python
from src.models.engine import ModelEngine
import yaml

# بارگذاری پیکربندی
with open('config/settings.yaml', 'r') as f:
    config = yaml.safe_load(f)

# ایجاد engine (مدل‌ها به صورت خودکار بارگذاری می‌شوند)
engine = ModelEngine(config=config)
engine.load_metadata()
engine.load_xgb_models()
engine.load_lstm()
engine.load_patchtst()
```

### 7. اجرای تست
```bash
pytest tests/
```

### 8. اجرای backtest
```bash
python main.py --backtest
```

### 9. اجرای live trading
```bash
python run_live_trading.py
```

## ⚠️ نکات مهم

1. **مدل‌ها از قبل آموزش دیده‌اند**: نیازی به آموزش مجدد نیست
2. **داده‌ها موجود هستند**: می‌توانید مستقیماً از مدل‌ها استفاده کنید
3. **پیکربندی‌ها حفظ شده‌اند**: تمام تنظیمات بهینه موجود است
4. **Optuna databases**: می‌توانید نتایج بهینه‌سازی را بررسی کنید

## 🔍 بررسی صحت بازسازی

```bash
# بررسی تعداد مدل‌ها
find models -name "*.joblib" | wc -l
find models -name "*.pt" | wc -l

# بررسی حجم داده‌ها
du -sh data/raw/
du -sh data/processed/

# بررسی پیکربندی‌ها
ls -lh config/
```

## 📊 آمار بکاپ

برای آمار کامل به فایل `SYSTEM_INFO.txt` مراجعه کنید.

## 🆘 عیب‌یابی

اگر مشکلی در بازسازی داشتید:

1. بررسی کنید که تمام فایل‌های مدل موجود هستند
2. بررسی کنید که requirements.txt نصب شده است
3. بررسی لاگ‌ها در `logs/`
4. به مستندات در فایل‌های `.md` مراجعه کنید

## ✅ چک‌لیست بازسازی

- [ ] بکاپ استخراج شد
- [ ] محیط مجازی ایجاد شد
- [ ] وابستگی‌ها نصب شدند
- [ ] مدل‌ها بررسی شدند
- [ ] پیکربندی‌ها بررسی شدند
- [ ] تست‌ها اجرا شدند
- [ ] سیستم آماده استفاده است
