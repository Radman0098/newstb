# 📊 راهنمای استفاده از گراف قیمت Real-Time

## ✅ ویژگی‌ها

سیستم ترسیم گراف قیمت real-time برای مانیتورینگ معاملات زنده:

- **کندل‌های قیمت**: نمایش OHLC با رنگ‌بندی (سبز/قرمز)
- **حجم معاملات**: نمایش Volume در زیر گراف
- **پوزیشن‌های باز**: نمایش خطوط ورود، SL و TP
- **سیگنال‌ها**: نمایش نقاط سیگنال‌های خرید/فروش
- **قیمت فعلی**: نمایش آخرین قیمت
- **به‌روزرسانی خودکار**: به‌روزرسانی هر N ثانیه

---

## 🚀 نحوه استفاده

### 1. فعال‌سازی در Live Trading

```bash
python3 scripts/mt5_auto_trader.py \
  --config config/settings.hybrid.tuned.yaml \
  --symbol XAUUSD \
  --timeframe M1 \
  --live --confirm-live \
  --enable-chart \
  --chart-update-interval 5.0
```

### 2. پارامترها

- `--enable-chart`: فعال‌سازی ترسیم گراف
- `--chart-update-interval`: فاصله به‌روزرسانی (ثانیه) - پیش‌فرض: 5.0

### 3. مسیر ذخیره گراف

گراف‌ها در پوشه `charts/` ذخیره می‌شوند:

- `{SYMBOL}_{TIMEFRAME}_latest.html` - آخرین نسخه (همیشه به‌روز)
- `{SYMBOL}_{TIMEFRAME}_{TIMESTAMP}.html` - نسخه‌های با timestamp

---

## 📋 مشاهده گراف

### روش 1: باز کردن فایل HTML

```bash
# باز کردن آخرین گراف
xdg-open charts/XAUUSD_M1_latest.html

# یا در مرورگر
firefox charts/XAUUSD_M1_latest.html
```

### روش 2: Auto-refresh در مرورگر

گراف‌های Plotly به صورت interactive هستند و می‌توانید:
- Zoom in/out
- Pan
- Hover برای دیدن جزئیات
- نمایش/مخفی کردن خطوط

برای auto-refresh، می‌توانید از extension مرورگر استفاده کنید یا فایل را هر چند ثانیه refresh کنید.

---

## 🎨 جزئیات گراف

### کندل‌ها
- **سبز**: کندل صعودی (Close > Open)
- **قرمز**: کندل نزولی (Close < Open)

### پوزیشن‌ها
- **خط سبز نقطه‌چین**: نقطه ورود BUY
- **خط قرمز نقطه‌چین**: نقطه ورود SELL
- **خط قرمز نقطه**: Stop Loss
- **خط سبز نقطه**: Take Profit

### سیگنال‌ها
- **مثلث سبز رو به بالا**: سیگنال خرید
- **مثلث قرمز رو به پایین**: سیگنال فروش
- **اندازه**: متناسب با Confidence

### حجم
- **نوارهای رنگی**: حجم معاملات
- **رنگ**: متناسب با جهت کندل

---

## ⚙️ تنظیمات پیشرفته

### تغییر تعداد کندل‌ها

در `price_chart_service.py`:
```python
max_candles: int = 500  # تغییر به تعداد مورد نظر
```

### تغییر فاصله به‌روزرسانی

```bash
--chart-update-interval 2.0  # هر 2 ثانیه
--chart-update-interval 10.0  # هر 10 ثانیه
```

### تغییر تم گراف

در `price_chart_service.py`:
```python
template="plotly_dark"  # تم تیره
# یا
template="plotly"  # تم روشن
```

---

## 🔧 عیب‌یابی

### گراف به‌روزرسانی نمی‌شود

1. بررسی لاگ:
```bash
tail -f logs/mt5_auto_trader_*.log | grep -i chart
```

2. بررسی فایل:
```bash
ls -lh charts/XAUUSD_M1_latest.html
```

3. بررسی دسترسی:
```bash
ls -ld charts/
```

### گراف خالی است

1. بررسی داده‌های MT5:
```bash
# بررسی فایل market
cat "/home/asr/.wine/drive_c/Program Files/MetaTrader 5/MQL5/Files/stb_market.txt"
```

2. بررسی اتصال:
```bash
# بررسی IPC health
python3 -c "from src.execution.mt5_bridge import MT5Bridge; b = MT5Bridge(); print(b.ipc_health())"
```

---

## 📊 مثال استفاده

### شروع با گراف

```bash
# توقف بات فعلی
./scripts/stop_trading_bot.sh

# شروع با گراف
python3 scripts/mt5_auto_trader.py \
  --config config/settings.hybrid.tuned.yaml \
  --symbol XAUUSD \
  --timeframe M1 \
  --live --confirm-live \
  --enable-chart \
  --chart-update-interval 5.0 \
  > logs/mt5_auto_trader_with_chart.log 2>&1 &
```

### مشاهده گراف

```bash
# باز کردن در مرورگر
firefox charts/XAUUSD_M1_latest.html &

# یا auto-refresh
watch -n 5 'xdg-open charts/XAUUSD_M1_latest.html'
```

---

## 🎯 نکات مهم

1. **Performance**: به‌روزرسانی هر 5 ثانیه مناسب است. کمتر از 2 ثانیه ممکن است CPU را زیاد مصرف کند.

2. **Storage**: هر به‌روزرسانی یک فایل جدید ایجاد می‌کند. برای جلوگیری از پر شدن دیسک، می‌توانید فایل‌های قدیمی را پاک کنید:
```bash
# پاک کردن فایل‌های قدیمی‌تر از 1 روز
find charts/ -name "*.html" -mtime +1 -delete
```

3. **Multiple Symbols**: برای هر نماد/تایم‌فریم یک گراف جداگانه ایجاد می‌شود.

4. **Background Thread**: سرویس گراف در background thread اجرا می‌شود و تأثیری روی performance معاملات ندارد.

---

**تاریخ ایجاد**: 2025-12-17  
**نسخه**: STB5

