# خلاصه رفع مشکل PCI برای وای‌فای

## 🔍 بررسی‌های انجام شده

### 1. بررسی PCI Devices
- ✅ 56 دستگاه PCI شناسایی شد
- ❌ هیچ دستگاه وای‌فای در PCI یافت نشد
- ❌ دستگاه `08:00.0` وجود ندارد

### 2. بررسی ماژول‌های کرنل
- ✅ ماژول `ath12k` بارگذاری شده است
- ✅ ماژول‌های وابسته (`mac80211`, `cfg80211`) موجود هستند
- ⚠️ ماژول‌ها بارگذاری شده‌اند اما دستگاه سخت‌افزاری برای استفاده وجود ندارد

### 3. بررسی Firmware
- ✅ Firmware وای‌فای نصب است (`linux-firmware`)
- ✅ فایل‌های firmware در `/lib/firmware/ath12k/` موجود هستند

### 4. اقدامات انجام شده
- ✅ Rescan کردن PCI bus
- ✅ بررسی دستگاه‌های PCI غیرفعال در sysfs
- ✅ تنظیم power management
- ✅ ایجاد فایل پیکربندی modprobe (`/etc/modprobe.d/ath12k.conf`)
- ✅ راه‌اندازی مجدد NetworkManager

## ⚠️ نتیجه

**مشکل اصلی**: دستگاه وای‌فای در سطح سخت‌افزاری (PCI) شناسایی نمی‌شود.

این نشان می‌دهد که:
1. **وای‌فای در BIOS غیرفعال است** (احتمال 95%)
2. یا دستگاه سخت‌افزاری مشکل دارد
3. یا نیاز به به‌روزرسانی BIOS است

## 🔧 راه‌حل نهایی

### روش 1: فعال‌سازی در BIOS (توصیه می‌شود)

1. **ریستارت سیستم**
2. **وارد BIOS شوید**: کلید `Delete` یا `F2` را هنگام بوت فشار دهید
3. **مسیر در BIOS MSI**:
   ```
   Advanced → Integrated Peripherals → WiFi → Enabled
   ```
   یا
   ```
   Settings → Advanced → Onboard Devices → WiFi → Enabled
   ```
4. **ذخیره و خروج**: `F10` را فشار دهید
5. **ریستارت سیستم**

### روش 2: بررسی اتصال فیزیکی

بررسی کنید که:
- آنتن‌های وای‌فای به پورت‌های `ANT1` و `ANT2` روی مادربرد وصل شده‌اند
- اتصالات محکم هستند

### روش 3: به‌روزرسانی BIOS

اگر وای‌فای در BIOS فعال است اما کار نمی‌کند:

1. به سایت MSI بروید: https://www.msi.com/Motherboard/X870-GAMING-PLUS-WIFI/support
2. آخرین نسخه BIOS را دانلود کنید
3. BIOS را به‌روزرسانی کنید

### روش 4: استفاده از USB WiFi Adapter (موقت)

اگر نیاز فوری به وای‌فای دارید:
- یک USB WiFi Adapter خریداری کنید
- سیستم به صورت خودکار آن را شناسایی می‌کند

## 📝 فایل‌های ایجاد شده

1. **`scripts/fix_pci_wifi.sh`**: اسکریپت رفع مشکل PCI
2. **`scripts/enable_wifi_bios_alternative.sh`**: اسکریپت روش‌های جایگزین
3. **`/etc/modprobe.d/ath12k.conf`**: پیکربندی ماژول ath12k

## ✅ بررسی پس از فعال‌سازی در BIOS

پس از فعال‌سازی وای‌فای در BIOS، این دستورات را اجرا کنید:

```bash
# بررسی دستگاه‌های PCI
lspci | grep -i network

# بررسی اینترفیس‌های شبکه
ip link show

# بررسی وضعیت NetworkManager
nmcli device status

# اجرای اسکریپت رفع مشکل
sudo /home/asr/Desktop/stb/scripts/fix_pci_wifi.sh
```

## 🔍 اطلاعات سیستم

- **مادربرد**: MSI X870 GAMING PLUS WIFI (MS-7E47)
- **نسخه BIOS**: 2.A53 (بر اساس لاگ‌های قبلی)
- **کرنل**: 6.17.9-76061709-generic
- **سیستم عامل**: Pop!_OS

## 📞 پشتیبانی

اگر مشکل حل نشد، این اطلاعات را جمع‌آوری کنید:

```bash
# اطلاعات کامل سیستم
sudo lspci -vvv > pci_info.txt
sudo dmesg > kernel_messages.txt
journalctl -u NetworkManager > network_manager.log
sudo dmidecode > system_info.txt
```

---

**تاریخ**: 2025-12-18  
**وضعیت**: نیاز به فعال‌سازی در BIOS



